# Copy to .env in the repo root if you prefer env-based keys.
# You can also set provider / key / model / base URL in the app (toolbar key icon).
# Placeholders only — never commit a real key.

OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# OPENAI_MODEL=gpt-4o
# ANTHROPIC_MODEL=claude-sonnet-4-20250514
# OPENAI_BASE_URL=
# ANTHROPIC_BASE_URL=

